name: Rust

on:
  push:
    branches: [ master ]

defaults:
  run:
    working-directory: amb_back

jobs:
  build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose